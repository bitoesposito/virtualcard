export const environment = {
  production: true,
  apiUrl: 'https://domain.com/api' // address with protocol://address.com:{port exposed from the server},
  // if you're using SSL instead, you'll need to change the final part of the url to "/api" (e.g. "https://domain.com/api")
};